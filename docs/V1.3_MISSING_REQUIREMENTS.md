# V1.3 Missing Requirements

本文件只列出当前缺失或待确认内容，不自行猜测补齐。

## 已确认项

以下内容已由产品方确认，可以作为后续 V1.3 工程实现前提：

1. 122 题数量正确。
2. T01 / T02 正确。
3. A / B / C / D / E 模块数量正确。
4. 8 类岗位分支完整。
5. MVP 固定抽题规则能抽出 34 题。
6. 每题都有选项。
7. 每个选项都有 `mainScore`。
8. `mainScore` 范围合法。
9. `companyScores` / `roleScores` 合法。
10. `riskTags` / `motivationTags` 合法。

## 题库与内容

1. 122 题是否已经工程化为 V1.3 专用 JSON 文件。
2. A 模块最终题干和选项文案是否产品确认。
3. B 模块最终题干和选项文案是否产品确认。
4. C 模块最终题干和选项文案是否产品确认。
5. D 模块最终题干和选项文案是否产品确认。
6. E 模块 8 类岗位分支最终题干和选项文案是否产品确认。
7. T01 / T02 的最终用户可见文案是否确认。
8. 固定抽题列表对应的 questionId 是否进入 V1.3 配置校验。

## 评分配置

1. `companyEntryWeights` 是否最终确认。
2. `roleEntryWeights` 是否最终确认。
3. `companyMotivationFit` 是否最终确认。
4. `roleMotivationFit` 是否最终确认。
5. `companyRolePairScore` 是否最终确认。
6. 未配置组合默认 `pairScore = 80` 是否最终确认。
7. `companyScores` 缺省为 2.5 是否最终确认。
8. `roleScores` 缺省为 2.5 是否最终确认。
9. 四象限判断的分数边界是否最终确认。
10. `65-74` 有条件适配区间是否最终确认。

## 风险与解释

1. 风险规则触发条件是否需要更精细。
2. `riskTags` 的业务解释是否最终确认。
3. `motivationTags` 的业务解释是否最终确认。
4. 每类目标公司对应的典型风险文案是否最终确认。
5. 每类目标岗位对应的典型风险文案是否最终确认。
6. 背景准入弱但行为适配高的解释模板是否需要单独配置。
7. 准入强但行为适配低的解释模板是否需要单独配置。
8. 结果页风险提示是否需要单独配置文件。
9. 行动建议是否需要单独配置文件。

## 结果页与转化

1. 结果页文案是否需要单独配置。
2. “猎头季哥怎么看”是否需要多套模板。
3. 最多 3 条行动建议的生成规则是否最终确认。
4. 公众号二维码文件是否存在。
5. 公众号二维码路径 `/public/qrcode-wechat.jpg` 是否最终确认。
6. 二维码不存在时的占位提示文案是否最终确认。
7. 是否继续沿用公众号关键词承接。
8. 是否需要保留 V1.2 preview 的样本切换能力作为内部验收工具。

## 数据与运营

1. 是否需要后端数据收集。
2. 是否需要 CSV 导出。
3. 是否需要事件埋点。
4. 是否需要记录用户目标公司和目标岗位选择分布。
5. 是否需要记录结果页停留和转化行为。
6. localStorage 是否继续作为第一阶段唯一 session 存储。

## 兼容与验收

1. 是否需要微信内置浏览器兼容测试。
2. 是否需要 iOS Safari 真机测试。
3. 是否需要 Android 微信浏览器真机测试。
4. 是否需要 375px / 390px / 414px 移动端专项验收。
5. 是否需要灰度入口。
6. 是否需要线上 preview 密码或隐藏入口。

## 工程边界

1. Goal Fit 是否完全独立于 V1.2 文件命名。
2. Goal Fit 是否需要新的统一 loader。
3. Goal Fit 是否需要独立 sample answer 生成器。
4. Goal Fit 是否需要独立 session migration 规则。
5. 是否需要删除或隐藏当前首页 V1.2 试用入口。
